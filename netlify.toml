[build]
  functions = "netlify/functions"
  publish = "public"
  command = "npm ci --production"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server/:splat"
  status = 200
  force = true
